# dshgw 配置示例（M58 形态：dshgw 是 aigw 的子进程，以同一个非特权账号运行）
#
# 两种用法：
#  1) 由 aigw 生成（推荐）：aigw 在启动 dshgw 前把这份配置写到自己的数据目录
#     （<aigw 数据根>/dshgw/config.yaml），路径全部由 aigw 解析成绝对路径；
#  2) 手工跑 `dshgw serve`：默认配置路径是部署根的 ./dshgw.yaml（`-config` 可覆盖）。
#
# 关于路径（M63，见 docs/deployment-layout.md）：
#   * 数据一律落在部署根的 ./data 之下；下面的相对路径以**进程工作目录**为基准，
#     加载时会归一成绝对路径。要写到别处请写绝对路径（`..` 不允许）。
#   * 运行时安装（dsh/node、bwrap、TLS 证书）不是数据，必须是绝对路径；留空则读
#     DSHGW_NODE / DSHGW_DSH_ROOT 环境变量。
#   * 本示例给出的是"单机部署"值：先用 `dshgw doctor` 看它在你机器上缺什么。
#
# 已删除的键（旧 root/systemd/nginx 形态）：deploy.systemd_dir / nginx_dir /
# nginx_include_path / nginx_binary / dshgw_binary / corepack_bin / worker_unit /
# worker_unit_bwrap / worker_slice / gateway_unit / dsh_user_prefix / public_listen /
# gateway_group / isolation，以及顶层 tls。严格解码器会拒绝它们。
public_host: chat.tirisen.hk
portal_port: 32600
tenant_port_lo: 32601
tenant_port_hi: 32799
worker_port_lo: 32100
worker_port_hi: 32299
listen: 127.0.0.1:3099          # dshgw 自己的监听地址；无 nginx 时租户端口由它直接服务
edge_port_header: X-DSHGW-Port
max_header_bytes: 131072
max_sessions: 10000
aigw_base_url: http://192.168.190.86:8088
# 租户 dsh 一次请求里允许累积的 base64 图片载荷（M68，默认 7340032 = 7 MiB）。必须低于 aigw 的
# server.max_body_bytes（默认 10485760）：网关对请求体是有界读取，超限时截断成 JSON 解析错误，
# 而不是回一句「请求太大」。设了它 dsh 会把最旧的图片换成占位符，请求继续能成。
# image_request_max_bytes: 7340032
validate_timeout: 5s
session_ttl: 168h
key_revalidate: off              # off | per-request | interval:<seconds>
dsh_enforce: login               # login | per-request | interval:<seconds>
# 控制台「启用/停用 DSH」用的本地通道默认在 <state_dir>/admin.sock（rootless 形态下它
# 属于当前账号，只接受同 UID 连接）；想放到别处再显式给出：
# admin_socket: /run/user/1000/dshgw/admin.sock
login_rate:
  requests: 10
  window: 1m
directory_picker: clamp          # clamp | browse
plugin_browser_fs: on
workspace_seed: [work]
reserved_names: [login, dshgw]
state_dir: ./data/dshgw           # 数据根；下面未写出的 state 路径都由它派生
# 运行时安装（本机实际位置；也可以用 DSHGW_NODE / DSHGW_DSH_ROOT 环境变量）
dsh:
  node_bin: /home/winger/.local/node-v22.23.1-linux-x64/bin/node
  bin_js: /home/winger/.local/dsh-0.1.2-rc.1/lib/bin.js
  current_link: /home/winger/.local/dsh-0.1.2-rc.1
deploy:
  # 目录选择器插件没有默认值：directory_picker 为 clamp 时**必须**给出，否则配置加载失败。
  # 本仓库自带一份：cmd/dshgw/plugin/picker-clamp.js（相对部署根即可）。
  plugin_path: ./cmd/dshgw/plugin/picker-clamp.js
  gateway_user: winger           # 网关自己以谁运行（aigw 的账号）
  worker_user: winger            # 所有租户 worker 以谁运行：必须是已存在的非 root 账号
  bwrap_bin: /usr/bin/bwrap
  # 工作区在租户沙箱里的短路径（M79）。留空 = 只有宿主长路径一个视图（HOME 与 /etc/passwd 视图都用它）。
  # 设成 /workspace 时：工作区同时绑在长路径与短路径上，HOME/~、目录选择器、终端、文件管理器与变更
  # 审阅都以短路径为准，新会话的路径与提示符随之变短；长路径保留绑定，因为网关自己的状态
  # （registry、dsh 已写下的会话/工作区记录）仍然用它。该值不需要宿主上存在（bwrap 在空 tmpfs 根里
  # 自己造），但不能是 /、/usr 之类运行时树、/home 之类被隐藏的根，也不能与 state_dir / tenant_root /
  # workspace_root / 插件目录互相包含 —— 那些在加载期就会被拒绝。
  sandbox_workspace: /workspace
  # 门户与租户公开端口的绑定地址。默认 loopback：对外暴露是显式决定（并且需要 TLS）。
  public_listen: 127.0.0.1
# 无 nginx 后由 dshgw 自己终止 TLS；两项都留空表示明文（仅限可信网络）。
# tls:
#   certificate: /path/fullchain.pem
#   certificate_key: /path/privkey.pem

# 飞书登录（M61）：让被绑定的人用飞书进入自己的租户，而不是粘贴 API Key。
#
# dshgw 自己不接触飞书：应用、App Secret 与重定向 URL 都属于 aigw，aigw 确认身份后签发一张
# 一次性票据；这里只验票。因此本块不需要 app_id/app_secret，也不需要第二个回调地址。
#
# 监督形态（aigw 拉起本进程）下这两项由 aigw 自动写入生成的配置，运维无需填写；
# 独立运行 dshgw 时按下面手工填成与 aigw 相同的值。
feishu:
  enabled: false
  # 浏览器可见的 aigw 登录入口（aigw 侧 feishu.callback_url 的 origin + /feishu/login）。
  aigw_login_url: http://192.168.190.86:8090/feishu/login
  # 必须与 aigw 的 feishu.ticket_secret 相同（aigw 留空时会由 credentials_key 派生并注入本文件）。
  ticket_secret: ""

# SSH 工作区（M64）：租户在自己的 dsh 里浏览/新建远端目录，网关把选中的远端目录挂进该租户的
# 工作区，于是它可以直接作为一个工作区打开。
#
# 分工是刻意这样切的：
#   * ssh 那一半（列目录、建目录、探测）在租户沙箱内由插件用**该租户自己的密钥**完成；
#   * 挂载那一半必须由本进程在沙箱外完成 —— 租户沙箱里既没有 /dev/fuse，宿主 root 在它的
#     user namespace 里也没有映射，所以 setuid 的 fusermount3 同样失效，mount(2) 一定被拒
#     （实测见 docs/design/m64-ssh-workspace.md §3）。因此 profile 不必新增设备或 capability，
#     隔离口径不变，只多出「为每个活动挂载点做一次 --bind-try」。
#
# 两侧靠该租户 DSH home 里的文件信箱通信（<dsh_home>/ssh-requests|ssh-replies）：
# 各自只能写自己的目录，所以不需要新端口、也不需要令牌。
#
# 密钥就是边界，所以**一账号一把**：租户能读自己那份 id_rsa，「这个账号能 ssh 到哪些主机」
# 就由发给它的密钥决定。密钥来源只有两个 —— 运维预置的 identity_dir/<账号>，或账号自己在
# 界面上传（上传后网关不再动它，见 identity-managed 标记）。**没有共享密钥来源**：早期版本有
# 一个 identity_source，本机曾把它指向部署账号自己的 ~/.ssh/id_rsa，于是 8 个租户各拿到一份
# 运维私钥的副本，而该密钥在本机 authorized_keys 里 —— 租户可以从沙箱 ssh 回宿主，直接成为部署
# 账号。该键已删除（配置里出现会直接拒绝启动）；identity_dir 也不允许落在本进程账号的 ~/.ssh 里。
# hosts 白名单只是防跑偏，不是安全边界。
#
# 别名清单也是**一账号一份**（ssh_config_dir/<账号>）：账号还没有 ~/.ssh/config 时用它做初始
# 种子，之后这份 config 归该账号所有 —— 租户插件（「我的主机」）在里增删条目，网关挂载时读的
# 也是它。**没有全局来源**：早期版本有一个 ssh_config_source，本机曾指向运维自己的
# ~/.ssh/config，等于把运维认识的全部主机交给每个租户，已删除（配置里出现该键会直接拒绝启动）；
# ssh_config_dir 也不允许落在本进程账号的 ~/.ssh 里。
#
# 前置：宿主装 sshfs（sudo apt install -y sshfs），且本进程的运行账号能非交互 ssh 到目标主机。
ssh_workspaces:
  enabled: false                  # 打开后：消费信箱、执行挂载、给租户渲染插件行
  mount_subdir: ssh               # 挂载点前缀：<workspace>/<mount_subdir>/<host>/<远端路径>
  # ssh_bin: /usr/bin/ssh         # 留空 = 走 PATH
  # sshfs_bin: /usr/bin/sshfs     # 留空 = 走 PATH（启用时找不到就拒绝启动）
  # identity_dir: /etc/dshgw/ssh/keys             # 按账号：<dir>/<账号>（0600），一账号一把
  # ssh_config_dir: /etc/dshgw/ssh/configs        # 按账号：<dir>/<账号>=别名清单（0644）；留空 = 账号初始无别名
  hosts: []                       # 空 = 允许手输（仍受 host 正则约束）；非空 = 白名单
  connect_timeout: 10s
  poll_interval: 2s               # 信箱轮询间隔
  max_entries: 1000               # 一次远端目录列出的上限
  sshfs_options: [reconnect, ServerAliveInterval=15, ServerAliveCountMax=3, idmap=user]
  # allow_other/allow_root 会被代码丢弃：所有租户 worker 共用一个 uid，共享挂载等于跨账号可读。
  disable_auto_remount: false     # true = 启动时不按记录重挂（挂载是内核状态，不跨进程存活）

# 侧栏「账号行」（M67）：租户 dsh 的侧栏底部多一行——登录的人叫什么（飞书名，取不到回退账号名，
# 再回退租户名），以及一个「退出」按钮。退出只撤销**本租户**的会话并把浏览器送回门户登录页，
# 同一浏览器里其它租户的登录态不受影响。
#
# 这一行的两个数据面是网关自己的、挂在**租户 origin** 下的路径：
#   GET  /dshgw/session/   → {ok, value:{tenant, account, feishu_name, name}}
#   POST /dshgw/logout/    → 303 到门户登录页（必须带本租户 origin；GET 是 405）
# 两者都要求该租户自己的会话 cookie，并与 worker 请求走同一道鉴权链（key 复验 + dsh 授权复核）；
# 本开关关闭时整个 /dshgw/ 命名空间返回 404，客户端那一行也就不出现。
#
# 名字来自 aigw（只有它知道飞书绑定），所以监督形态下这个开关由 aigw 生成；独立部署照抄
# aigw 的 dshgw.account_card.enabled 即可。旧租户的账号名在下次「启用/轮换密钥」时补上。
account_card:
  enabled: false                  # 打开后：渲染租户侧插件行 + 提供上面两个端点

# 租户侧 web 插件（M75）：每个账号的 dsh 默认就带三块面板——
#   web-tty          终端：真正的 PTY（node-pty 从 dsh 发行版解析），跑在该账号自己的 bwrap 沙箱里
#   workspace-files  工作区文件：浏览/预览/编辑/上传/下载/改名/删除，全部夹紧在 root 子树内
#   git-diff         变更：只读地看仓库改动（两栏 diff，永不改 .git/index）
#
# **三个都默认开启**（这就是本块存在的意义：账号不必有人手改它的 profile）。想看更窄的面就显式关：
# 关掉即从每个租户的 profile 里移除该行（已存在的租户在下次 worker 启动时生效）。
#
# 前置：三个插件目录要部署在 plugin_path 同级的 web-tty/、workspace-files/、git-diff/ 下
# （仓库里的 ./cmd/dshgw/plugin/ 已是这个形状）；`dshgw doctor` 逐个检查。开着但没部署时：
# 建户/轮换密钥直接失败（会把路径说清楚），既有租户启动只丢那一行并写 warning —— 一个可选面板
# 不该让账号起不来。
#
# 运行期状态按账号隔离，落在该账号自己的 DSH home：
#   <DshHome>/plugin-state/{web-tty.trace.jsonl, workspace-files.trace.jsonl,
#                           git-diff.trace.jsonl, git-diff.cache.json}
# 插件目录（共享、可能 root 拥有）里不留任何状态，git-diff 的扫描缓存也不跨账号共享。
# root_label 是两个工作区面板对 root 的显示名（留空 = 工作区）；其余数值旋钮用插件自身默认值。
# 细节见 docs/dshgw.md §7f 与 docs/design/m75-tenant-plugins.md。
tenant_plugins:
  web_tty:
    enabled: true
  workspace_files:
    enabled: true
  git_diff:
    enabled: true
  # root_label: 工作区
