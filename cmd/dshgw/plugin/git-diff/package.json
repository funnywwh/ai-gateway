{
  "name": "dshgw-git-diff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only git change review panel for the dsh web GUI: a «变更» View in the session's main area, left the changed-file list and right a two-column side-by-side diff, over dsh's own authenticated RPC channel",
  "exports": {
    ".": "./index.js",
    "./client": "./client.js"
  },
  "scripts": {
    "build": "node build-client.mjs",
    "test": "node --test --test-force-exit test/*.test.mjs"
  },
  "dsh": {
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-connection",
        "@deepseek-ai/dsh-client-ui-renderer"
      ]
    }
  }
}
